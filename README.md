# WhoisOSINT
WhoisOSINT is a CLI toolkit that enriches domains with WHOIS, DNS, HTTP, TLS, tracker, and GeoIP data, exports XLSX, and generates a Markdown report that clusters domains by shared infrastructure signals to support OSINT investigations. It includes rate limiting, caching, and safe parsing for scale.
